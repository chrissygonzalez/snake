export enum Direction {
    UP = 'UP',
    DOWN = 'DOWN',
    LEFT = 'LEFT',
    RIGHT = 'RIGHT'
}

export enum GameStates {
    INITIAL = 'INITIAL',
    RUNNING = 'RUNNING',
    PAUSED = 'PAUSED',
    ENDED = 'ENDED'
}